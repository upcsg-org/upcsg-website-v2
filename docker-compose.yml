version: '3.9'

services:
    app:
        image: milaymaxell/upcsg:latest
        ports:
            - '3000:3000'
        environment:
            NODE_ENV: production
        command: ['npm', 'run', 'start']
